<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shieldapp1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShieldApp1">
        <activity android:name=".LocationOwner.Barisal"></activity>
        <activity android:name=".LocationOwner.Dinajpur" />
        <activity android:name=".LocationOwner.Sylhet" />
        <activity android:name=".LocationOwner.Jessore" />
        <activity android:name=".LocationOwner.comilla" />
        <activity android:name=".LocationOwner.Chittagong" />
        <activity android:name=".LocationOwner.dhaka" />
        <activity android:name=".LocationOwner.raj" />
        <activity android:name=".User.UserDashboard" />
        <activity android:name=".Common.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>